%h2
  ユーザー一覧
%table.table.table-striped
  %thead
    %tr
      %th
        メールアドレス
      %th
        直近ログイン日時
      %th
        最終ログイン日時
      %th 
        作成日時
      %th 
        更新日時
      %th 
        郵便番号
      %th
        住所
      %th
        氏名
      %th
  %tbody
    - @users.each do |user|
      %tr
        %td
          = user.email
        %td
          = disp_normal_date user.current_sign_in_at 
        %td
          = disp_normal_date user.last_sign_in_at
        %td
          = disp_normal_date user.created_at
        %td
          = disp_normal_date user.updated_at
        %td
          = user.postal_code 
        %td
          = user.address_level1             
          = user.address_level2             
          = user.address_line1             
          = user.address_line2             
        %td
          = user.family_name             
          = user.given_name              
        %td
          - if user.can_delete?
            = link_to "削除", [:admin, user], method: :delete, data: {confirm: "削除して良いですか？"}, class: 'btn btn-danger'          