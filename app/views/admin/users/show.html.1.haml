%h2
  ユーザー情報参照
= simple_form_for [:admin, @user], wrapper: :disabled_form do |f|
  = f.input :email
  = f.input :password
  = f.input :sign_in_count
  = f.input :current_sign_in_at
  = f.input :last_sign_in_at
  -# = link_to "一覧に戻る", admin_products_path, class: 'btn btn-default' 