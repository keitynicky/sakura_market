%h2
  商品一覧
= link_to "新規追加", new_admin_product_path, class: 'btn btn-primary'
%table.table.table-striped
  %thead
    %tr
      %th
        商品名
      %th
        説明文
      %th
        価格
      %th 
        ソート順
      %th 
        商品画像
      %th
  %tbody
    - @products.each do |product|
      %tr.clickable-row{"data-link" => edit_admin_product_path(product)}
        %td
          = link_to product.name, edit_admin_product_path(product)
        %td
          = product.description 
        %td
          = product.price
        %td
          = product.sort
        %td
          %div.img-thumbnail
            = image_tag show_photo_product_path(product), size: "50x40"
        %td
          - if product.can_delete?
            = link_to "削除", [:admin, product], method: :delete, data: {confirm: "削除して良いですか？"}, class: 'btn btn-danger'
