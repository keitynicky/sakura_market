%h2
  商品一覧
%table.table.table-striped
  %thead
    %tr
      %th
        商品名
      %th
        説明文
      %th
        価格
      %th 
        ソート順
      %th 
        商品画像
  %tbody
    - @products.each do |product|
      %tr
        %td
          = product.name
        %td
          = product.description 
        %td
          = product.price
        %td
          = product.sort
        %td
          %div.img-thumbnail
            = image_tag show_photo_product_path(product), size: "50x40"